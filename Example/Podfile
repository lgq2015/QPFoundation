use_frameworks!

target 'QPFoundationExample' do
  platform :ios, '8.0'
  pod 'QPFoundation', :path => '../'
  pod 'Masonry', '~> 1.1.0'
end

target 'QPGenerateNetworkingSources' do
  use_frameworks! :linkage => :static
  platform :osx, '10.8'
  pod 'QPFoundation', :path => '../'
end
